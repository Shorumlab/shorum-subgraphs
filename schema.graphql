# FeeDistributor

type BackerFeeFollowModule @entity {
  # Contract address
  id: ID!

  hub: Bytes!

  moduleGlobals: Bytes!

  totalDistributors: BigInt!

  updateAt: BigInt!
}

type ProfileDistributor @entity {
  
  id: ID!

  createdBlock: BigInt!

  profileId: BigInt!

  distributor: Bytes! 
  
  timestamp: BigInt!
}

